# Build 0.2.2

Hotfix for mixed Safari/PWA caches and missing character graphics.

- cache-safe character renderer: `js/characters.js`;
- versioned module and stylesheet URLs;
- standalone `wiki.html`;
- PWA cache `merge-arcana-v3-0.2.2`;
- renderer regression tests.
